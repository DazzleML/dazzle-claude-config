<!-- GENERATED by scripts/gen-config-docs.py -- do not edit by hand.
     The words come from dazzle_claude_config/settings-explanations.json;
     the defaults and environment variables from dazzle_claude_config/userconfig.py.
     Edit those, then re-run the script. -->

# Configuration

ccs reads its preferences from `~/claude/ccs-config.json`. Every setting also has an environment variable, which wins for a single shell, and most have a command-line flag, which wins for a single run:

    explicit flag  >  environment variable  >  config file  >  built-in default

The file is optional. A machine with no config at all behaves safely -- ccs prompts rather than guessing, and never spends money without being asked.

## Keeping the file current

A config file freezes at the settings of whatever version wrote it. `ccs setup update` adds the ones this version knows, at their documented defaults, and changes nothing you have set:

    ccs setup update --dry-run     # show exactly what would be added
    ccs setup update               # add them
    ccs setup update --explain sync_removals    # what one setting means, offline

You do not need this page to answer a question about one setting -- `--explain <name>` prints the same words in your terminal, with no network and no config file. This page exists for reading them all at once.

## Every setting


| Setting | Default | Environment variable |
|---|---|---|
| [`on_divergence`](#on_divergence) | `"prompt"` | `CCS_ON_DIVERGENCE` |
| [`difftool`](#difftool) | `null` | `CCS_DIFFTOOL` |
| [`ai_merge_command`](#ai_merge_command) | `null` | `CCS_AI_MERGE_COMMAND` |
| [`interactive`](#interactive) | `true` | `CCS_INTERACTIVE` |
| [`status_detail`](#status_detail) | `"auto"` | `CCS_STATUS_DETAIL` |
| [`status_max_lines`](#status_max_lines) | `30` | `CCS_STATUS_MAX_LINES` |
| [`fetch`](#fetch) | `true` | `CCS_FETCH` |
| [`fetch_timeout`](#fetch_timeout) | `15` | `CCS_FETCH_TIMEOUT` |
| [`require_current`](#require_current) | `false` | `CCS_REQUIRE_CURRENT` |
| [`auto_pull`](#auto_pull) | `false` | `CCS_AUTO_PULL` |
| [`sync_removals`](#sync_removals) | `"untouched"` | `CCS_SYNC_REMOVALS` |
| [`merge_inject`](#merge_inject) | `"ask"` | `CCS_MERGE_INJECT` |


### on_divergence

**Default:** `"prompt"` &middot; **Environment:** `CCS_ON_DIVERGENCE` &middot; **One of:** `force`, `prompt`, `skip`

What to do when a file has diverged two ways. 'prompt' asks; 'skip' leaves it alone and reports it; 'force' overwrites, which is destructive.

### difftool

**Default:** `null` &middot; **Environment:** `CCS_DIFFTOOL`

The git difftool to force for `ccs diff` and `ccs merge`. Unset means resolve it from your git config, falling back to scanning for one when the configured default is not on disk.

### ai_merge_command

**Default:** `null` &middot; **Environment:** `CCS_AI_MERGE_COMMAND`

Shell command for AI-assisted merge; unset disables the option entirely. It receives the base, your version and the output path as $CCS_BASE, $CCS_OURS and $CCS_OUT. Left unset on purpose: AI costs money, so it is opt-in per machine.

### interactive

**Default:** `true` &middot; **Environment:** `CCS_INTERACTIVE`

Whether ccs may ask you questions. False suppresses every prompt and uses on_divergence directly. Also inferred automatically when input is not a terminal, so an automated run never hangs waiting for an answer nobody can give.

### status_detail

**Default:** `"auto"` &middot; **Environment:** `CCS_STATUS_DETAIL` &middot; **One of:** `auto`, `compact`, `long`

How much detail `ccs status` prints. 'auto' shows the per-file breakdown until it would exceed status_max_lines, then falls back to one line per entry; 'long' always shows every file; 'compact' always shows one line per entry.

### status_max_lines

**Default:** `30` &middot; **Environment:** `CCS_STATUS_MAX_LINES`

The line budget 'auto' spends before collapsing to one line per entry. Raise it if you would rather always see every file; lower it on a small terminal.

### fetch

**Default:** `true` &middot; **Environment:** `CCS_FETCH`

Whether `ccs status` contacts the remote before reporting. It does, so that 'in sync with origin/main' is a claim about the remote rather than about whenever you last fetched. It touches remote-tracking refs only -- no branch, index or working tree. Turn it off for an air-gapped or metered machine; --no-fetch does the same for one run.

### fetch_timeout

**Default:** `15` &middot; **Environment:** `CCS_FETCH_TIMEOUT`

Seconds before a fetch is treated as failed and the pull state is reported as unknown. One repo and one remote: 15 is generous for a hosted remote and short enough that a dead network does not make `status` feel hung.

### require_current

**Default:** `false` &middot; **Environment:** `CCS_REQUIRE_CURRENT`

What happens when your checkout is behind its remote. By default apply and collect warn and proceed -- 'install what I have here, now' is a legitimate intent and nothing is lost. True turns that warning into a refusal, for people who want the pull-first loop enforced. --require-current does it for one run.

### auto_pull

**Default:** `false` &middot; **Environment:** `CCS_AUTO_PULL`

Whether `ccs status` fast-forwards your checkout before reporting, when the fetch finds it behind and the move is safe. Strictly fast-forward only: a diverged branch or a file in the way is reported, never merged, rebased or stashed. status only -- apply and collect keep their warning, see require_current. --pull / --no-pull per run.

### sync_removals

**Default:** `"untouched"` &middot; **Environment:** `CCS_SYNC_REMOVALS` &middot; **One of:** `all`, `never`, `untouched`

What to do about a file the payload RETIRED that this machine still carries. 'untouched' moves it into the backup directory only when your copy still matches a committed version, i.e. holds nothing of yours; a copy you edited is reported and kept instead, because staging away work the payload never had is the one thing this tool refuses to do quietly. 'all' stages every retired file; 'never' only reports. Nothing is ever deleted in place.

### merge_inject

**Default:** `"ask"` &middot; **Environment:** `CCS_MERGE_INJECT` &middot; **One of:** `always`, `ask`, `never`

What `ccs merge --relaunch` may do for a resumed file when your merge tool regenerates its output pane (BeyondCompare does): ccs can reopen the tool and paint your saved work back into the pane, which takes the keyboard for about a second. 'ask' (the default) warns and asks each time; 'always' warns and proceeds; 'never' leaves such files closed. Never happens without a console, and never without --relaunch.

---

*This page is generated. To correct the wording of a setting, edit `dazzle_claude_config/settings-explanations.json` and run `python scripts/gen-config-docs.py`. To change a default or add a setting, edit `dazzle_claude_config/userconfig.py` as well.*
