{
 "test_command": [
  "python",
  "-m",
  "pytest",
  "-q",
  "-p",
  "no:cacheprovider",
  "tests/test_adoption_merge.py",
  "tests/test_honoured_deletion.py"
 ],
 "mutations": [
  {
   "id": "M1",
   "file": "dazzle_claude_config/basefind.py",
   "original": "    return hits < len(bl) / 2\n",
   "mutated": "    return hits <= len(bl) / 2\n",
   "note": "Block-swap threshold boundary"
  },
  {
   "id": "M2",
   "file": "dazzle_claude_config/basefind.py",
   "original": "        difflib.SequenceMatcher(None, b, t, autojunk=False).ratio() >= ratio for t in tl))\n",
   "mutated": "        difflib.SequenceMatcher(None, b, t, autojunk=False).ratio() > ratio for t in tl))\n",
   "note": "Similarity boundary >= -> >"
  },
  {
   "id": "M3",
   "file": "dazzle_claude_config/basefind.py",
   "original": "        if all(i in b2o for i in range(i1, i2)) and any(base[i].strip() for i in range(i1, i2)):\n            out.append(Region(i1, i2, [ours[b2o[i]] for i in range(i1, i2)]))\n",
   "mutated": "        if any(i in b2o for i in range(i1, i2)) and any(base[i].strip() for i in range(i1, i2)):\n            out.append(Region(i1, i2, [ours[b2o.get(i, i)] for i in range(i1, i2)]))\n",
   "note": "all->any: partly-kept region accepted"
  },
  {
   "id": "M6",
   "file": "dazzle_claude_config/basefind.py",
   "original": "            out += [OURS_MARK, *r.ours, BASE_MARK, *r.ours, SEP_MARK, THEIRS_MARK]\n",
   "mutated": "            out += [OURS_MARK, *r.ours, BASE_MARK, SEP_MARK, THEIRS_MARK]\n",
   "note": "Wrapped hunk loses its base pane"
  },
  {
   "id": "M7",
   "file": "dazzle_claude_config/basefind.py",
   "original": "        if at < 0:\n            missing += 1\n            continue\n",
   "mutated": "        if at < 0:\n            continue\n",
   "note": "missing not counted"
  },
  {
   "id": "M10",
   "file": "dazzle_claude_config/basefind.py",
   "original": "    yset = {l.strip() for l in y}\n    bset = {l.strip() for l in base} if base is not None else set()\n",
   "mutated": "    yset = {l for l in y}\n    bset = {l.strip() for l in base} if base is not None else set()\n",
   "note": "Dropped .strip() on the other side"
  },
  {
   "id": "M12",
   "file": "dazzle_claude_config/basefind.py",
   "original": "        if t != \"equal\":\n            n += (i2 - i1) + (j2 - j1)\n",
   "mutated": "        if t != \"equal\":\n            n += (i2 - i1)\n",
   "note": "distance asymmetric"
  }
 ]
}