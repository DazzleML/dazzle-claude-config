# Mutation sweep: v0.5.1 remote ergonomics (`ccs git`, auto_pull, remote leg)

## Environment

| | |
|---|---|
| Project | dazzle-claude-config, uncommitted v0.5.1 wave on top of `cca558b` |
| Targets | `cli.py` (`_split_git_passthrough`, `_run_git_verb`, the auto_pull block), `gitops.py` (`ff_update`, `remote_url`), `render.py` (`branch_name`, `remote_host`, `humanize_remote`) |
| Suite at baseline | 314 green (26 in `tests/test_remote_ergonomics.py` + 15 in `tests/test_remote_awareness.py` focused) |
| Generation | Mode 1 (fresh subagent, zero tools, source inline; suite unseen). 12 mutants |
| Harness | `mutation_sweep.py` (skill copy), per-mutant cache namespace, survivor re-verify |
| Python / platform | 3.12 / Windows 11 (Git Bash) |
| Date | 2026-08-25 |

## Round 1 kill table (spec `v051-spec.json`, report `v051-report.json`)

| id | target | mutant | verdict |
|---|---|---|---|
| M1 | cli split | `i + 1 <` -> `<=` (IndexError on trailing value flag) | killed |
| M2 | cli split | `argv[i + 1:]` -> `argv[i:]` (verb leaks into git args) | killed |
| M3 | cli git verb | `-C` after subcommand (runs in caller's cwd) | killed |
| M4 | auto_pull | `want is None` -> `is not None` (opt-in dead, flag inverted) | killed |
| M5 | auto_pull | `want and fetched` -> `or` (pull without opt-in) | killed |
| M6 | auto_pull | tuple packing `"" if ok else msg` inverted (reason dropped) | **survived** |
| M7 | ff_update | no-upstream returns `True` (phantom success) | **survived** |
| M8 | ff_update | `first[0]` -> `first[-1]` ("Aborting" instead of the reason) | **survived** |
| M9 | remote_url | dropped `.strip()` (trailing newline) | **survived** |
| M10 | remote_host | `[:-4]` -> `[:-3]` (trailing dot) | killed |
| M11 | remote_host | host/path swapped in scp form | killed |
| M12 | humanize_remote | behind-hints inverted | killed |

## Triage -- every survivor KILLABLE, loop closed same run

| id | verdict | killing test (written now, in `tests/test_remote_ergonomics.py`) |
|---|---|---|
| M6 | killable -- no test asserted the refusal REASON reached the output | `test_dirty_file_refusal_carries_gits_first_line` |
| M7 | killable -- guard unreachable via the CLI (behind is None without an upstream) but it is the public primitive's contract | `test_ff_update_without_upstream_refuses` |
| M8 | killable -- same dirty-file test pins git's FIRST line ("local changes"), which the last line ("Aborting") lacks | `test_dirty_file_refusal_carries_gits_first_line` |
| M9 | killable, not don't-care -- `remote_host`'s defensive `.strip()` masks it today, but the primitive's contract should not depend on every future consumer stripping | `test_remote_url_comes_back_clean` |

No equivalents; `equivalents.md` unchanged (its old `cli.py` section is stale by hash and was not fed to the generator).

## Round 2 (spec `v051-spec-round2.json`, report `v051-report-round2.json`)

M6, M7, M8, M9 re-applied against the extended suite: **4/4 killed**. Total 12/12.

## Post-sweep checks

- `git status --short` delta vs pre-sweep capture: only the four intended spec/report files. Production files byte-restored (harness-verified per mutant).
- Full suite after the closure tests: **317 passed**.

## Caveats (standing)

A kill proves the suite noticed *this* change, not that it tests the right thing -- and a clean sweep is evidence about the mutations tried, not proof of suite completeness.
