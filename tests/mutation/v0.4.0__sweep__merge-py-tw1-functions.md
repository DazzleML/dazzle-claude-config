# Mutation sweep: merge.py TW1 functions (v0.4.0 working tree + TW1)

**Environment:** dazzle-claude-config, staged-0.4.0 + unstaged TW1 (merge.py object 083c8c867e2b); Python 3.13.2; Windows 10 19045; harness: test-mutation skill's mutation_sweep.py (from plzwork's skill dir); generation mode: **1 (fresh subagent, zero tools)**; 2026-08-21.

**Scope:** the three TW1-changed functions (_head_candidates, _head_items, two_way_labels). Test command: pytest -q tests/test_merge.py.

## Rounds

- **Red-green audit** (single-mutation special case, run first): reverting the TW1 diff sent 3 of 5 new tests red -- covers_directory_entries, ignores_diverged_seed_entries, head_items_infer_a_base are ANCHORS. dir_entry_resolved_worktree_not_labeled is an INVARIANT GUARD (passes vacuously pre-fix). Restore hash-verified.
- **Round 1:** 12 mutants; 6 apply-failed -- every multi-line snippet. Root cause: the spec was written with LF while merge.py is CRLF; the dispatcher's pre-verify used a text-mode read that normalized newlines and reported 12 false 'unique' hits. The harness's byte-exact matching caught the bad method. Spec rewritten with file EOL.
- **Round 2** (clean, zero anomalies): 4 killed, 8 survived -> triage -> 5 killer tests written -> 9 killed, 3 survived.
- **Single-mutant re-verify:** m10's first killer proved to be an invariant guard (files_differ filters the naive fixture before the mutated line runs); rewritten to route through an uncommitted-worktree edit; killed on re-verify.

## Final kill table (12 mutants)

| id | mutation | verdict | killed by |
|---|---|---|---|
| m1 | or->and on partial-entry skip | killed | test_partial_entries_are_skipped_not_crashed (new) |
| m2 | single-file entries enter directory axis | killed | same test's candidate-level assertion -- **dispatcher triaged this 'equivalent'; round 2 disproved the claim** |
| m3 | drop is_file on directory members | survived | -- |
| m4 | invert git-repo check | killed | pre-existing suite |
| m5 | drop EOL normalization, HEAD equality skip | killed | test_head_axis_ignores_eol_only_difference (new) |
| m6 | swap ours/theirs into infer_base | killed | test_phantom_rejection_measures_base_to_ours_deletions (new) |
| m7 | invert render/plugins refusal | killed | test_head_axis_refuses_render_strategy_items (new) |
| m8 | directory members install to entry root | killed | pre-existing suite (repo_dest assertion) |
| m9 | flip existence guard in two_way_labels | killed | test_two_way_labels_covers_directory_entries (new, anchor) |
| m10 | drop EOL normalization, guard live side | killed | test_two_way_guard_ignores_eol_only_difference (rewritten) |
| m11 | no-base files silently safe | killed | pre-existing no-base test |
| m12 | and->or over-reports one-sided as two-way | survived | -- |

**Score: 10/12 killed.**

## Survivor triage (none unlabeled)

- **m3 -- equivalent.** d.modified guarantees both sides exist at diff time; only a filesystem race between diff and yield could expose the dropped guard, and a race is outside the testable contract. Recorded in equivalents.md.
- **m12 -- killable, killer already written, deliberately deferred.** The killing test exists (test_two_way_labels_dir_entry_one_sided_live_unchanged) and is xfail(strict)-marked behind the S1 interlock: the scenario that distinguishes and/or requires infer_base to return an equality base, which the S1 bug prevents. When plzwork's S1 commit removes the @S1_INTERLOCK line, this test activates and m12 dies. Not recorded as equivalent -- it is the one mutant whose killer is time-delayed by an inter-machine handshake.

## Caveats (verbatim per the skill)

A kill proves the suite noticed *this* change, not that it tests the right thing -- and **a clean sweep is evidence about the mutations tried, not proof of suite completeness.**

Suite after close-the-loop: 149 passed + 1 xfailed (was 140 at 0.4.0 staging).
