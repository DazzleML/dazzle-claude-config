{
  "_generated": "2026-09-01, mode 1 (fresh-context subagent, zero tools, source text only); M10's original widened by the operator to be unique (the bare line also occurs in _wait_for_tool)",
  "test_command": ["python", "-B", "-m", "pytest", "-q", "-p", "no:cacheprovider", "tests/test_inject_flow.py", "tests/test_inject_driver.py", "tests/test_tool_capability.py"],
  "mutations": [
    {"id": "M1", "file": "dazzle_claude_config/merge.py", "original": "base = Path(exe.replace(\"\\\\\", \"/\")).name.lower()", "mutated": "base = Path(exe.replace(\"\\\\\", \"/\")).name", "note": "Drops the .lower() normalization, so an executable name differing only in case no longer matches the lowercase keys in EXE_RESUME/registry lookups."},
    {"id": "M2", "file": "dazzle_claude_config/merge.py", "original": "    cap = by_name.get(name)\n    if cap:", "mutated": "    cap = by_name.get(name)\n    if cap is not None:", "note": "None-vs-truthiness -- an empty-string resume cap (falsy but not None) now short-circuits as a hit instead of falling through to the executable-based lookup."},
    {"id": "M3", "file": "dazzle_claude_config/merge.py", "original": "return tool_resume(name, registry) == RESUME_PRELOADS", "mutated": "return tool_resume(name, registry) != RESUME_PRELOADS", "note": "Comparison flip inverts the reopen-safety check, marking every declared-safe tool unsafe and every unclassified tool safe to reopen."},
    {"id": "M4", "file": "dazzle_claude_config/merge.py", "original": "if not want:", "mutated": "if want:", "note": "Guard flip in the platform-applicability check -- an os-scoped profile now applies unconditionally everywhere, while a platform-agnostic profile now never applies."},
    {"id": "M5", "file": "dazzle_claude_config/merge.py", "original": "if not isinstance(profile, dict) or not _profile_applies(profile):", "mutated": "if not isinstance(profile, dict) and not _profile_applies(profile):", "note": "or->and swap lets a well-typed profile that simply doesn't apply on this platform slip through unrejected, silently promoting an inapplicable inject profile."},
    {"id": "M6", "file": "dazzle_claude_config/merge.py", "original": "if snap.get(\"open\"):", "mutated": "if not snap.get(\"open\"):", "note": "Inverts the core relaunch-safety guard, letting the tool relaunch into an already-open file -- the hidden-second-session data-loss scenario the function exists to prevent."},
    {"id": "M7", "file": "dazzle_claude_config/merge.py", "original": "if not ask(item, tool):", "mutated": "if ask(item, tool):", "note": "Inverts the consent check so injection proceeds when the user declines the prompt and is refused when they approve it."},
    {"id": "M8", "file": "dazzle_claude_config/merge.py", "original": "and merged.read_bytes() != kept:", "mutated": "and merged.read_bytes() == kept:", "note": "Comparison flip on the finally-block safety net -- the pre-inject sidecar is restored only when the file is unchanged, never in the actual case where an unverified paint corrupted it."},
    {"id": "M9", "file": "dazzle_claude_config/merge.py", "original": "and not discard and prof is not None):", "mutated": "and discard and prof is not None):", "note": "Boolean flip inverts which branch handles a resumed+relaunch item -- routes to the injection flow only when discard was requested."},
    {"id": "M10", "file": "dazzle_claude_config/merge.py", "original": "            if rc is not None:\n                res.tool_exit[item.label] = rc", "mutated": "            if rc:\n                res.tool_exit[item.label] = rc", "note": "None-vs-truthiness -- a successful tool exit code of 0 is falsy, so it is no longer recorded into res.tool_exit after the injection flow."},
    {"id": "M11", "file": "dazzle_claude_config/inject.py", "original": "for line in reversed(text.splitlines()):", "mutated": "for line in text.splitlines():", "note": "Drops the reversal, breaking the documented 'LAST JSON line wins' contract -- now returns the first valid JSON line, which can be a stray warning printed before the real result."},
    {"id": "M12", "file": "dazzle_claude_config/inject.py", "original": "if result is None:", "mutated": "if not result:", "note": "None-vs-truthiness -- a driver result of {} (a valid but empty JSON object) is wrongly treated as 'no result line found' instead of proceeding to be filled in with ok/rc."},
    {"id": "M13", "file": "dazzle_claude_config/inject.py", "original": "if value is not None and value != \"\":", "mutated": "if value is not None or value != \"\":", "note": "and->or swap lets a None value (which satisfies value != \"\") pass through, appending the literal string 'None' as a driver argument instead of being skipped."}
  ]
}
