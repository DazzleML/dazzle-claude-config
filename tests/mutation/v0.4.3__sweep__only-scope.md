# Mutation sweep: `--only` scoping (0.4.3, slice B7)

| | |
|---|---|
| Project / commit | dazzle-claude-config, working tree on `caf478e` + the uncommitted B7 change |
| Targets | `dazzle_claude_config/syncmap.py`: `only_scope`, `rel_in_scope`, `scope_diff` |
| Test command | `python -m pytest -q -p no:cacheprovider tests/test_only_scope.py` |
| Python / platform | 3.12.0 / Windows-11-10.0.26200 |
| Generation mode | **1 -- fresh subagent**, source inline, zero tools, 9 mutants |
| Date | 2026-08-22 |
| Specs / reports | `v043-b7-spec.json` / `-report.json`; round 2 `-spec-round2.json` / `-report-round2.json` |

## Kill table (round 1: 18 tests)

| ID | Mutation | Verdict |
|---|---|---|
| M1 | entry match regresses to a string prefix | killed |
| M2 | sub-entry branch uses a string prefix (`skills-extra/x` on `skills`) | **survived** |
| M3 | off-by-one slice keeps the leading slash | killed |
| M4 | backslash normalisation dropped | killed |
| M5 | exact-match case lost | killed |
| M6 | `rel_in_scope` regresses to a string prefix | killed |
| M7 | exact-file case dropped | killed |
| M8 | `denied_live` left unfiltered in `scope_diff` | **survived** |
| M9 | `is None` -> `not sub_prefix` | **survived** |

## Triage

| ID | Verdict | Test |
|---|---|---|
| M2 | killable | parametrised case `("dotclaude/skills-extra/x", "dotclaude/skills") -> (False, None)` |
| M8 | killable | `test_scope_diff_cuts_every_list` |
| M9 | **equivalent** | recorded in `equivalents.md`: an empty sub-prefix cannot be produced |

Round 2: 2 / 2 killed. Final: 8 killed, 1 equivalent.

A kill proves the suite noticed *this* change, not that it tests the right thing -- and **a clean sweep is evidence about the mutations tried, not proof of suite completeness.**
