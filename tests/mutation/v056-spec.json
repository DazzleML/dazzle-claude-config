{
 "test_command": ["python", "-m", "pytest", "-q", "-x",
                  "tests/test_untracked_attribution.py",
                  "tests/test_remote_awareness.py"],
 "mutations": [
  {"id": "M1", "file": "dazzle_claude_config/cli.py",
   "original": "                held = bool(wrong_dir or r.removals_pending or r.mismatched)",
   "mutated": "                held = bool(wrong_dir or r.mismatched)",
   "note": "A pending removal no longer counts as held back, so the summary claims live matches the checkout while a file is waiting to be retired -- the exact lie #29 removed."},
  {"id": "M2", "file": "dazzle_claude_config/cli.py",
   "original": "                held = bool(wrong_dir or r.removals_pending or r.mismatched)",
   "mutated": "                held = bool(r.removals_pending or r.mismatched)",
   "note": "A direction-skipped file no longer counts as held back."},
  {"id": "M3", "file": "dazzle_claude_config/cli.py",
   "original": "                held = bool(wrong_dir or r.removals_pending or r.mismatched)",
   "mutated": "                held = bool(wrong_dir and r.removals_pending and r.mismatched)",
   "note": "or->and: only all three at once count as held back, so almost every held-back run claims a match."},
  {"id": "M4", "file": "dazzle_claude_config/cli.py",
   "original": "                        if args.verb == \"apply\" and kind == \"one-sided\" \\\n                                and evidence.startswith(\"live ahead\"):\n                            wrong_dir[key] = \"live is ahead -- nothing to apply; `ccs collect` it\"",
   "mutated": "                        if args.verb == \"apply\" and kind == \"one-sided\" \\\n                                and evidence.startswith(\"checkout ahead\"):\n                            wrong_dir[key] = \"live is ahead -- nothing to apply; `ccs collect` it\"",
   "note": "Direction flipped: apply now skips the files it should copy and copies the ones it should skip."},
  {"id": "M5", "file": "dazzle_claude_config/cli.py",
   "original": "                        if args.verb == \"apply\" and kind == \"one-sided\" \\\n                                and evidence.startswith(\"live ahead\"):",
   "mutated": "                        if args.verb == \"apply\" and kind != \"one-sided\" \\\n                                and evidence.startswith(\"live ahead\"):",
   "note": "Kind check inverted: attributed one-sided files stop being guarded, unattributed ones start."},
  {"id": "M6", "file": "dazzle_claude_config/cli.py",
   "original": "    if shown.returncode != 0 or not shown.stdout:\n        return \"local snap\", (\"not committed in the checkout yet -- history \"",
   "mutated": "    if shown.returncode != 0 and not shown.stdout:\n        return \"local snap\", (\"not committed in the checkout yet -- history \"",
   "note": "or->and: a path git resolves to empty output is no longer treated as uncommitted, and the code proceeds to attribute against nothing."},
  {"id": "M7", "file": "dazzle_claude_config/render.py",
   "original": "        hint = \"ccs status --pull\" if fetched else \"ccs git fetch to confirm\"",
   "mutated": "        hint = \"ccs git fetch to confirm\" if fetched else \"ccs status --pull\"",
   "note": "Hints swapped: a confirmed-behind checkout is told to re-fetch, stale knowledge is told to pull."},
  {"id": "M8", "file": "dazzle_claude_config/cli.py",
   "original": "    if not diffs and behind:",
   "mutated": "    if not diffs or behind:",
   "note": "and->or: the behind-only verdict fires whenever there is drift, replacing the real drift report."}
 ]
}
