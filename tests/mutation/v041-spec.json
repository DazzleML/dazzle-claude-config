{
 "test_command": ["python", "-m", "pytest", "-q", "-p", "no:cacheprovider",
                  "tests/test_base_inference.py", "tests/test_merge.py", "tests/test_cli.py",
                  "tests/test_apply.py", "tests/test_collect.py"],
 "mutations": [
  {"id": "M1", "file": "dazzle_claude_config/merge.py",
   "original": "    if head and head not in shas:\n        shas.insert(0, head)          # `git log -- path` omits a TREESAME merge commit at HEAD",
   "mutated": "    if head and head not in shas:\n        shas.append(head)          # `git log -- path` omits a TREESAME merge commit at HEAD",
   "note": "Rule 1: HEAD is appended last instead of inserted at the front, so on a score tie the oldest candidate wins the `[0]` pick rather than HEAD."},
  {"id": "M2", "file": "dazzle_claude_config/merge.py",
   "original": "    if all(s[4] for s in scored) and len(scored) == 1:",
   "mutated": "    if all(s[4] for s in scored) and len(scored) <= 0:",
   "note": "Rule 1 guard removed: a single-commit history where HEAD is the only candidate is no longer refused, so adoption is mistaken for a real sync point."},
  {"id": "M3", "file": "dazzle_claude_config/merge.py",
   "original": "        if cand == ours_n:\n            return (p.stdout, sha[:7])                               # rule 2",
   "mutated": "        if cand == ours_n and cand != theirs_n:\n            return (p.stdout, sha[:7])                               # rule 2",
   "note": "Rule 2: an exact-ours candidate that also equals theirs no longer short-circuits, so a proof-not-guess base is dropped into the scoring contest."},
  {"id": "M4", "file": "dazzle_claude_config/merge.py",
   "original": "        eq_theirs = cand == theirs_n\n        is_rejected = False",
   "mutated": "        eq_theirs = cand == ours_n\n        is_rejected = False",
   "note": "Rule 3 exemption keys off the wrong side: theirs-equal candidates lose their phantom immunity and the true base is rejected whenever live deleted >= 3 lines."},
  {"id": "M5", "file": "dazzle_claude_config/merge.py",
   "original": "    others = [s for s in at_best if not s[4]]\n    nearest = (others or at_best)[0]",
   "mutated": "    others = [s for s in at_best if s[4]]\n    nearest = (others or at_best)[0]",
   "note": "Rule 3 tie-break inverted: on an exact distance tie the theirs-equal candidate now wins by unearned immunity instead of losing."},
  {"id": "M6", "file": "dazzle_claude_config/merge.py",
   "original": "    if nearest[3]:                                                   # rule 4\n        return None",
   "mutated": "    if nearest[3] and False:                                                   # rule 4\n        return None",
   "note": "Rule 4 removed: a phantom-rejected nearest candidate is returned as the base instead of refusing, turning refusals into silent wrong-base merges."},
  {"id": "M7", "file": "dazzle_claude_config/merge.py",
   "original": "            if n >= PHANTOM_MIN_LINES and ratio >= PHANTOM_RATIO:   # rule 5",
   "mutated": "            if n >= PHANTOM_MIN_LINES or ratio >= PHANTOM_RATIO:   # rule 5",
   "note": "Rule 5 and/or swap: a single deleted line theirs happens to retain now rejects the candidate, refusing valid bases wholesale."},
  {"id": "M8", "file": "dazzle_claude_config/merge.py",
   "original": "        if tag == \"delete\":\n            deleted += [l for l in b[i1:i2] if l.strip()]",
   "mutated": "        if tag in (\"delete\", \"replace\"):\n            deleted += [l for l in b[i1:i2] if l.strip()]",
   "note": "Phantom ratio counts `replace` opcodes too, so our own ${CLAUDE_USER_DIR} rewrites inflate the ratio and a true ancestor gets rejected."},
  {"id": "M9", "file": "dazzle_claude_config/cli.py",
   "original": "    if base_n == _normalize_eol(ours):\n        return \"one-sided\", f\"checkout ahead; live == {found[1]}\"",
   "mutated": "    if base_n == ours:\n        return \"one-sided\", f\"checkout ahead; live == {found[1]}\"",
   "note": "Dropped EOL normalization on the ours comparison: a CRLF-vs-LF live file is misreported as two-sided instead of checkout-ahead."},
  {"id": "M10", "file": "dazzle_claude_config/cli.py",
   "original": "    if shown.returncode != 0 or not shown.stdout:\n        return \"local snap\", \"never committed -- the checkout copy is a local snapshot; collect replaces it\"",
   "mutated": "    if shown.returncode != 0 and not shown.stdout:\n        return \"local snap\", \"never committed -- the checkout copy is a local snapshot; collect replaces it\"",
   "note": "or/and flip: a path that git cannot show falls through to infer_base with empty `theirs` instead of reporting `local snap`."},
  {"id": "M11", "file": "dazzle_claude_config/cli.py",
   "original": "                        elif args.verb == \"collect\" and kind == \"one-sided\" \\\n                                and evidence.startswith(\"checkout ahead\"):",
   "mutated": "                        elif args.verb == \"collect\" and kind == \"one-sided\" \\\n                                and evidence.startswith(\"live ahead\"):",
   "note": "Direction block: collect now skips live-ahead files (the ones it should collect) and collects checkout-ahead files, undoing the other machine's work."},
  {"id": "M12", "file": "dazzle_claude_config/cli.py",
   "original": "    left = _normalize_eol(rp.read_bytes()) if rp.is_file() else b\"\"\n    right = _normalize_eol(lv.read_bytes()) if lv.is_file() else b\"\"",
   "mutated": "    left = rp.read_bytes() if rp.is_file() else b\"\"\n    right = lv.read_bytes() if lv.is_file() else b\"\"",
   "note": "_print_file_diff drops EOL normalization, so a CRLF-vs-LF identical file prints a whole-file diff and exits 1 instead of reporting identical and exiting 0."}
 ]
}
