# Mutation sweep: v0.5.2 wave (seed findings, decisions, directory seeds, write_box)

## Environment

| | |
|---|---|
| Project | dazzle-claude-config, uncommitted #26/#27/#28 wave on top of `84f1ccb` (v0.5.1) |
| Targets | `cli.py` (`_norm_sha`, `_seed_findings`), `seeddecisions.py` (load/keep/reset), `apply.py` (directory-seed branch), `boxconfig.py` (`write_box`) |
| Suite at baseline | 348 green (focused: test_seed_visibility 12, test_setup_doctor 12, test_dir_seed 6) |
| Generation | Mode 1 (fresh subagent, zero tools, source inline, suite unseen). 12 mutants |
| Harness | `mutation_sweep.py` (skill copy), per-mutant cache namespace, survivor re-verify |
| Python / platform | 3.12 / Windows 11 |
| Date | 2026-08-25 |

## Round 1 (spec `v052-spec.json`, report `v052-report.json`): 8 killed, 4 survived

Killed: M1 (CRLF normalization dropped), M2 (covered-set over-inclusion), M3 (commit-id vs blob-sha tuple element), M4 (kept-current/reopened flip), M7 (reset no-op), M8 (reseed without session backup), M9 (never-overwrite guard flip), M11 (validation errors ignored).

## Triage -- all four survivors KILLABLE, loop closed same run

| id | why it survived | killing test (written now) |
|---|---|---|
| M5 | tests covered file-level malformed JSON, not a RECORD with a wrong `decision` value -- which the mutant silently honours as a keep | `test_record_with_wrong_decision_field_is_flagged_malformed` |
| M6 | no test ever recorded TWO decisions; keep() clobbering the ledger passed a single-decision suite | `test_keep_merges_with_existing_decisions` |
| M10 | every test passed `--reseed` in forward-slash form; the dropped backslash normalization -- the form a Windows user actually types -- had no witness | `test_reseed_accepts_the_windows_backslash_form` |
| M12 | the CLI path is SHIELDED: the name is always inserted as a tag, and tag validation catches bad names redundantly -- so the name-check flip was invisible through `ccs setup box`. Killable (and worth killing) at the public write_box API: defense in depth, recorded as such | `test_write_box_api_validates_the_name_itself` |

No equivalents; `equivalents.md` unchanged.

## Round 2 (spec `v052-spec-round2.json`): M5, M6, M10, M12 all killed. Total 12/12.

## Post-sweep checks

- Full suite after closures: **352 passed** (348 + 4 closure tests).
- Working tree: production files byte-restored per mutant (harness-verified); tree delta = the intended spec/report/test additions only.
- One process anomaly, not harness-related: the M10 closure test was initially written through a shell heredoc and its backslashes were corrupted in transit (fourth such incident this project); repaired via the editor tool. The standing rule -- backslash-bearing content never goes through a heredoc -- is now absolute.

## Caveats (standing)

A kill proves the suite noticed *this* change, not that it tests the right thing -- and a clean sweep is evidence about the mutations tried, not proof of suite completeness.
