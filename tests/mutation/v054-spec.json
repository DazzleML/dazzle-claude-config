{
 "mutations": [
  {
   "id": "M01",
   "file": "dazzle_claude_config/migrate.py",
   "original": "    want = target.replace(chr(92), \"/\").strip(\"/\")\n    for entry in manifest.seed_entries():\n        if want in (entry.target, entry.repo) and entry_applies(entry, box_tags):\n            return entry",
   "mutated": "    want = target.replace(chr(92), \"/\").strip(\"/\")\n    for entry in manifest.seed_entries():\n        if want in (entry.target, entry.repo) or entry_applies(entry, box_tags):\n            return entry",
   "note": "and->or in find_seed_entry: an entry meant for another box can be named."
  },
  {
   "id": "M02",
   "file": "dazzle_claude_config/migrate.py",
   "original": "    return [(t, s) for t, s, *_ in findings\n            if s in (\"untouched-old\", \"open\", \"reopened\")]",
   "mutated": "    return [(t, s) for t, s, *_ in findings\n            if s in (\"untouched-old\", \"open\")]",
   "note": "Dropped 'reopened' from migratable states."
  },
  {
   "id": "M03",
   "file": "dazzle_claude_config/migrate.py",
   "original": "    live, seed = entry_bases(entry, checkout, roots, manifest.territories)\n    if not seed.is_file():",
   "mutated": "    seed, live = entry_bases(entry, checkout, roots, manifest.territories)\n    if not seed.is_file():",
   "note": "Swapped live/seed unpacking: every hash and check reads the wrong side."
  },
  {
   "id": "M04",
   "file": "dazzle_claude_config/migrate.py",
   "original": "    if not live.is_file():\n        raise MigrateError(\n            f\"{entry.target} does not exist on this box yet -- a plain \"",
   "mutated": "    if live.is_file():\n        raise MigrateError(\n            f\"{entry.target} does not exist on this box yet -- a plain \"",
   "note": "Inverted the live-file guard."
  },
  {
   "id": "M05",
   "file": "dazzle_claude_config/migrate.py",
   "original": "    keep_dir = Path(keep_root) if keep_root else Path(backup_root).parent / \"pre-migrate\"",
   "mutated": "    keep_dir = Path(keep_root) if keep_root is not None else Path(backup_root).parent / \"pre-migrate\"",
   "note": "None-vs-truthiness on keep_root: empty string becomes the cwd."
  },
  {
   "id": "M06",
   "file": "dazzle_claude_config/migrate.py",
   "original": "    result.reseeded = entry.target in r.reseeded\n    if not result.reseeded:",
   "mutated": "    result.reseeded = entry.target in r.reseeded\n    if result.reseeded:",
   "note": "Inverted the reseed-failure guard."
  },
  {
   "id": "M07",
   "file": "dazzle_claude_config/migrate.py",
   "original": "        why = next((f\"{p}: {e}\" for p, e in r.failed if p == entry.target),\n                   \"apply did not report the reseed\")",
   "mutated": "        why = next((f\"{p}: {e}\" for p, e in r.failed if p == target),\n                   \"apply did not report the reseed\")",
   "note": "Wrong variable: filters failures by the caller's string, not the resolved target."
  },
  {
   "id": "M08",
   "file": "dazzle_claude_config/migrate.py",
   "original": "    if keep.is_file() and sha256_of(keep) == result.pre_sha:\n        result.verified.append(f\"your pre-migration copy is intact at {keep}\")",
   "mutated": "    if keep.is_file() or sha256_of(keep) == result.pre_sha:\n        result.verified.append(f\"your pre-migration copy is intact at {keep}\")",
   "note": "and->or: the kept copy is 'intact' on mere existence."
  },
  {
   "id": "M09",
   "file": "dazzle_claude_config/migrate.py",
   "original": "    if result.ccs_backup is None:\n        result.problems.append(\"apply reported no backup directory\")",
   "mutated": "    if result.ccs_backup is None:\n        result.verified.append(\"apply reported no backup directory\")",
   "note": "A missing backup dir is recorded as a verification instead of a problem."
  },
  {
   "id": "M10",
   "file": "dazzle_claude_config/migrate.py",
   "original": "    if live.is_file() and sha256_of(live) == sha256_of(seed):\n        result.verified.append(\"the payload's version is live now\")",
   "mutated": "    if live.is_file() and sha256_of(live) == result.pre_sha:\n        result.verified.append(\"the payload's version is live now\")",
   "note": "A reseed that replaced nothing now reports success."
  },
  {
   "id": "M11",
   "file": "dazzle_claude_config/cli.py",
   "original": "            if answer in (\"k\", \"a\"):\n                mode = \"always\" if answer == \"a\" else \"until-changed\"",
   "mutated": "            if answer in (\"k\", \"a\"):\n                mode = \"always\" if answer == \"k\" else \"until-changed\"",
   "note": "The two keep modes are exactly reversed."
  },
  {
   "id": "M12",
   "file": "dazzle_claude_config/cli.py",
   "original": "                if rc == EXIT_CLEAN:\n                    decided += 1\n                break",
   "mutated": "                if rc != EXIT_CLEAN:\n                    decided += 1\n                break",
   "note": "Failed migrations count as decided; successful ones do not."
  },
  {
   "id": "M13",
   "file": "dazzle_claude_config/cli.py",
   "original": "                print(c(\"dim\", f\"stopped -- {decided} decided, \"\n                               f\"{len(todo) - i + 1} left\"))",
   "mutated": "                print(c(\"dim\", f\"stopped -- {decided} decided, \"\n                               f\"{len(todo) - i} left\"))",
   "note": "Off-by-one in the quit message's remaining count."
  },
  {
   "id": "M14",
   "file": "dazzle_claude_config/cli.py",
   "original": "                _seed_diff(entry, checkout, roots, manifest,\n                           getattr(args, \"tool\", None))\n                continue          # ask about the SAME file again",
   "mutated": "                _seed_diff(entry, checkout, roots, manifest,\n                           getattr(args, \"tool\", None))\n                break          # ask about the SAME file again",
   "note": "After a diff the walk advances instead of re-asking; the file is left undecided."
  }
 ],
 "test_command": [
  "python",
  "-m",
  "pytest",
  "-q",
  "-x",
  "tests/test_migrate_and_walk.py",
  "tests/test_seed_visibility.py"
 ]
}